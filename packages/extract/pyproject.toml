[project]
name = "shotclassify-extract"
version = "0.1.0"
requires-python = ">=3.11"
description = "ShotClassify extract package"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/shotclassify_extract"]
