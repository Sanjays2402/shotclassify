[project]
name = "shotclassify-route"
version = "0.1.0"
requires-python = ">=3.11"
description = "ShotClassify route package"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/shotclassify_route"]
