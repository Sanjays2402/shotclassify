# Tick 22 — 2026-06-23 04:07 PT — Cake (cron)

5 features shipped to `main` in one batch.

## Commits

- `7a124e0` feat(extract): cross-category percentage extractor into raw["percentages"]
- `b802bf1` feat(extract/chat): thread-reply marker detection into ChatFields.threads
- `cfe6b8e` feat(extract/receipt): subscription / recurring-charge detection into ReceiptFields.recurring
- `2a16808` feat(extract/code): secret/key-literal sniffing into CodeFields.suspected_secrets
- `58bf75d` feat(extract/code): type-annotation density into CodeFields.type_annotation_density

## Gate

- `make lint` (ruff at baseline 536, no NEW errors). Two fixups folded directly during initial write — B033 duplicate set items in percentages.py's label vocab (loss/rate), B005 multi-char lstrip on TS code.py's _count_ts_slots destructuring prefix.
- `make test` 5352 passed / 3 skipped in 132.84s. One flaky test (`test_webhook_egress_allowlist::test_suffix_entry_matches_subdomains`) failed in the FIRST gate run but PASSED in isolation immediately after AND on the gate re-run — test-isolation drift unrelated to my changes (webhook egress policy module, untouched).
- 300 new tests across the 5 features (66 + 58 + 66 + 58 + 52).

## Schema / wire-format updates

- New ReceiptFields slot: `recurring` (dict | None).
- New CodeFields slots: `suspected_secrets` (list of {kind, hint} dicts), `type_annotation_density` (float).
- New ChatFields slot: `threads` (list of {count, last_reply, sender?} dicts).
- One new cross-category raw key: `raw["percentages"]`.
- LLM wire format in `classify/client.py` updated for all five new slots so an LLM-supplied value survives the round trip.

## Roadmap

Tick 22 burns 5 items from the backlog (134, 130, 136, 99, 128). Refilled with 8 new items (137..144) so the backlog now has 23 open. The remaining backlog skews toward Chart / Document / UI mockup work (the categories we haven't touched yet) plus a handful of niche receipt / chat / error / code follow-ups.

## Disk-space note

Hit "No space left on device" on the first gate run -- `/var/folders` had only 125MiB free at 100% capacity. Freed **1.8 GiB** by removing:
- `/var/folders/.../T/pytest-of-sanjay/` (372MiB)
- `/var/folders/.../T/tmp*`, `cm-*`, `BlobRegistryFiles-*` (~24MiB)
- `~/Library/Caches/go-build/` (974MiB)
- `~/Library/Caches/gopls/` (310MiB)

Worth flagging to Sanjay: his `/System/Volumes/Data` was sitting at 100% (228 GiB used / 0 GiB free). Most of that is outside our control but the macOS GoLang cache had grown large enough to be the difference between "tests pass" and "OSError everywhere".

## Notable design decisions

1. **Percentages extractor's range matcher claims its span first** so the bare matcher doesn't steal endpoints. Labelled matcher excludes `-` from its separator class so a leading sign on the value (`Change -3%`) is preserved as the sign group instead of being eaten as separator. This caught `test_negative_signed_percent` failing on first run.

2. **Threads extractor's sender-attribution loop skips transcript-line lookalikes** (Thread:/Reply:/View/Last/Replying). The literal word `Thread` followed by `:` would otherwise register as a NAME sender for the NEXT marker. This caught `test_thread_tagged_colon` failing on first run.

3. **Recurring detector's multi-word patterns ordered FIRST** so "Semi-annual subscription" beats "Annual subscription" and "Biweekly subscription" beats "Weekly subscription" via Python first-match-wins ordering. This caught `test_semi_annual_subscription` failing on first run.

4. **Suspected-secrets SECURITY GUARANTEE**: FULL VALUE IS NEVER STORED; hint is REDACTED preview (first 4 + `...` + last 4 chars). Generic env vars without secret semantics (LOG_LEVEL/API_URL/API_VERSION) don't fire because key name MUST match the curated catalogue. Low-entropy values (DEBUG=true) filtered by 2+ char-class + 16+ char threshold for the uppercase matcher.

5. **Type-annotation density** excludes Python `self`/`cls` and TS `this` from both numerator and denominator because they're idiomatically untyped. Optional TS args (`y?: string`) recognised as typed via `?` in the param-name regex. Strictly-typed languages (Java/Kotlin/Go/Rust/etc) return 1.0 when any function-def shape is detected; `_FUNC_LIKE_RE` recognises BOTH keyword-led forms (`func`/`fn`/`fun`/`def`) AND access-modifier-led forms (`public X.Y foo()`) with optional return type between keyword and name.

## Lessons

- Test isolation drift on a 5000+ test suite is a real risk. The gate's first failure was on a webhook-egress test I didn't touch; running it alone proved it green. I shipped the batch.
- Disk-space pressure is now a chronic issue on this Mac mini. Each tick should consider clearing pytest tmp dirs proactively.
- Multi-word vocabulary catalogues need explicit longest-first ordering. Python regex first-match-wins means `Annual subscription` will steal the suffix of `Semi-annual subscription` unless the longer form is checked first.
