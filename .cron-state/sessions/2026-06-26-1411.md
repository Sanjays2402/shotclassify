# Tick 35 -- 2026-06-26 13:40 PT (Cake, cron)

FRONTEND OVERRIDE active. Shipped 5 frontend slices, all web/ TS/TSX/CSS,
zero Python touched.

## Shipped (5 commits, all on origin/main)
- b47b0c9 feat(web): keyboard cycle for the /stats time window (F79)
- 0962bf9 feat(web): copy a single shot as CSV on the detail page (F80)
- 3586635 feat(web): collapsible side rail on the shot-detail page (F77)
- ef202fc feat(web): live-ticker count pulse when a classification lands (F76)
- 6cca183 feat(web): shots-list shortcut legend in the command palette (F70)

## What each does
- F79: `w` cycles the /stats window 24h->7d->30d. New "stats" SHORTCUTS scope;
  page handler is input-guarded AND chord-guarded against `g w` (Webhooks).
  Pure nextStatsWindow(); persists via writeStatsWindow.
- F80: single-shot "Copy CSV" button on the detail header. Extracted
  csvCell / CSV_HEADERS / csvRow / toCsv into shot-export.ts as the single
  source of truth; shot-export-bulk.ts imports them now (BULK_CSV_HEADERS
  aliases CSV_HEADERS; toBulkCsv maps csvRow). Cross-module test proves the
  single row == the bulk row for the same shot. Bulk output byte-identical.
- F77: collapsible detail rail. lib/detail-rail.ts (SET of collapsed slot
  keys; parse/serialize/toggle, no-throw storage) + <CollapsibleSection>
  (grid-rows fold + rotating caret, body stays mounted, reduced-motion snap).
  UmpireControls + LabelTagsEditor gained `embedded` to drop their panel chrome.
- F76: live ticker glows cue-yellow when a count ticks up. lib/ticker-pulse.ts
  (didIncrease / increasedKeys -- first-observation, equal, decrease, non-finite
  never pulse). Ticker uses prev refs, adds a transient class, strips it after
  the keyframe. Sample data never pulses. globals.css keyframe + reduced-motion.
- F70: "Shots list" footer legend in the command palette exposing the `v`/`d`
  shortcuts. lib/command-palette.ts shotsScopeHints(SHORTCUTS) + shortLabelForHint().

## Gate (once, end of batch -- all green)
- `npx tsc --noEmit`: clean (whole web project).
- `npm test` (force-exit): 458 passed / 0 failed (420 baseline + 38 new).
- `npx next build`: compiled successfully; /shots + /stats still static,
  /shots/[id] dynamic.
- Zero Python touched -> pytest baseline cannot regress.

## Notes / carried forward
- Web test glob hangs after assertions pass (dangling handle in one suite).
  Use `npx tsx --test --test-force-exit lib/*.test.mts` for a clean 458/0.
  Documented in STATE.md stack snapshot.
- ruff: ~536 PRE-EXISTING errors from version drift, unrelated to this batch
  (zero Python touched). Still flagged for Sanjay; needs a `ruff check --fix`
  + pin bump as a separate non-override task.
- Backlog: F70/F76/F77/F79/F80 done. Refilled F81-F90 (10 new). Plenty open.
