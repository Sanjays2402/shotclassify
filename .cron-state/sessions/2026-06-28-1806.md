# Tick 46 — 2026-06-28 18:06 PDT

Frontend batch (override active). Shipped 5 slices, all `/keys` + `/stats` polish.

## Shipped
- **F140** (b2f1ff1) — extracted the /keys/[id] usage-sparkline geometry into a
  tested pure lib `key-sparkline` (cleanSeries/sparklineGeometry/summarizeSeries).
  empty/single-point/all-zero/NaN edges now covered; peak floors at 1; added a
  "Peak N on DAY" caption. 9 tests.
- **F141** (e01d07e) — detail-page sample now uses buildSnippet + a shared
  components/SnippetLangToggle, driven by the persisted snippet-lang pref. Fixed
  a broken unterminated-quote curl literal. /keys page reuses the same toggle.
- **F142** (d5975ea) — sanctioned backend touch: Repository.aggregate emits
  per-hour mean_confidence; stats ingest-tempo gains a dashed conf line on a
  right 0-100% axis + legend. TS type + demo synth + pytest.
- **F143** (e84c8b0) — detail-page rotate/revoke drop confirm() for the F136
  two-step inline confirm. Matches /keys list.
- **F144** (bdcdec4) — idle/never-used summary chips are now toggle filters
  (chipIsFilterable/toggleSummaryFilter/filterKeysByStatus) + Show-all banner.
  4 tests.

## Gate
- web: 764 tests pass, 0 fail; tsc clean.
- py: 6 aggregate tests pass; repository.py ruff = 21 (baseline-identical, no
  new errors). Repo-wide ruff backlog (537) pre-existing.
- pushed c723d68..bdcdec4 -> origin/main.

## Notes
- Detail page already had an inline Sparkline; F140 made its math testable
  rather than reinventing — good leverage.
- LSP "sampleCurl" / "NOW" diagnostics during edits were stale caches; tsc
  confirmed clean each time.
- 15 roadmap items remain (F145-F159); added F155-F159 fresh.
