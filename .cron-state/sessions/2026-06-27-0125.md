# Tick 38 session — 2026-06-27 01:25 PT (Cake, cron)

FRONTEND OVERRIDE active. Shipped 5 frontend slices, one commit each, gated once.

## Shipped
- F102 (98e9d0c) — "Filtering N of M deliveries" count line on /webhooks when the
  F92 status/event filter narrows the table. New pure deliveryFilterCountLabel(
  shown,total): null when nothing hidden, singular/plural aware, clamps over-range,
  truncates fractional, no-throw on non-finite. 5 tests appended to
  webhook-delivery-chips.test.mts (13->18).
- F101 (14a6c65) — clickable status legend with LIVE counts above the deliveries
  table (success/failed/pending, felt-green/red/amber). Each swatch toggles the F92
  status filter (active->clear); aria-pressed reflects state. New pure
  deliveryStatusCounts(): always the 3 known statuses in stable order, zero when
  absent, trims, hasOwnProperty tally (ignores unknown + prototype keys), safe on
  non-array. 4 tests appended (18->22).
- F104 (465de1e) — one-click facet reset on the command palette. New pure
  stripFacets(query) reuses parseFacets (removes exactly what it recognised, keeps
  residual free text, idempotent, trims, preserves unresolved class: value, safe on
  non-string). "Clear" button in the facet pill row applies it + refocuses input.
  7 tests appended to palette-facets.test.mts (13->20).
- F106 (68f231e) — /stats KPI cards name the active window in their sub-label
  (winLabel = labelForStatsWindow(hours) threaded into all 4 hints). Pure render.
- F100 (dc73a20) — "Tip: press G, then a letter." caption under the "Jump to a
  section" group header in the ? overlay (scopeOrder rows carry optional caption;
  section map renders it). Closes long-open F69/F90. Pure render.

Also marked F107 done — the notifications "N of M match" line was already shipped
(page.tsx ~L347 renders `${matched} of ${total} match` when filtersActive). No work
needed; recorded so it isn't re-picked as filler.

## Gate (once, whole batch)
- npx tsc --noEmit — clean (whole web project)
- npx tsx --test --test-force-exit lib/*.test.mts — 522 passed / 0 failed
- npx next build — compiled successfully in 5.1s; /shots, /stats, /webhooks static,
  /shots/[id] dynamic. ZERO Python touched -> pytest baseline cannot regress.

## Notes / carries
- Heeded tick-37's clobber lesson: ALL test additions to pre-existing *.test.mts
  files were done via patch (append), never write_file. Both files green standalone
  (22 + 20) and in the glob.
- Backlog ~45 open (above the <5 refill threshold) -> no refill this tick. Neighbours
  F103 (persist webhooks filter to URL), F105 (rail reset-to-defaults), F108 (webhooks
  empty-state -> EmptyState) are good next picks — they continue the same
  filter-consolidation / affordance arc.
- Repo-wide ruff still ~536 PRE-EXISTING errors from version drift (untouched by this
  batch). Flagged for Sanjay; out of scope for the frontend override.

Pushed c5a13ee..dc73a20 -> origin/main, verified HEAD == origin/main.
