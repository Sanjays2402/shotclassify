# Tick 32 session — 2026-06-25 22:10 PT (Cake, cron)

**Mode:** 20-min batch loop, FRONTEND override active (since 2026-06-23).
**Branch:** main. **Result:** 5 frontend slices shipped, pushed, verified.

## Shipped (5)
1. **F51** — persist the `/shots` page-size selector across visits.
   `lib/shots-page-size.ts` + `lib/shots-page-size.test.mts` (9 tests). Wired
   into the page-size `<select>` (load-on-mount + setLimitPersist writer).
   SHA `ef1be38`.
2. **F52** — name the active filters in the /shots copy-link toast.
   `describeShotsFilters()` / `shotsFilterParts()` / `copyLinkToastMessage()`
   appended to shots-deeplink.ts, reusing buildShotsQuery's active-filter rules.
   Wired into CopyViewLinkButton. 8 new tests (shots-deeplink.test.mts -> 30).
   SHA `1c7236b`.
3. **F58** — reflect the /shots filter into the browser tab title.
   `lib/shots-doc-title.ts` (reuses F52's shotsFilterParts) + 5 tests. Page
   sets document.title in a debounced effect, restores prior title on unmount.
   SHA `ef6c7bd`.
4. **F59** — command-palette recent rows show "viewed 3m ago".
   `lib/relative-time.ts` + 8 tests. Palette `Recent` type gains viewedAt;
   captures one `now` on open; full timestamp on hover. SHA `9fe0a49`.
5. **F50** — command-palette resting discoverability hint.
   `paletteRestingHint(resting, recentCount)` in command-palette.ts + 4 tests.
   Renders a Sparkle-prefixed tip under the nav only on a bare palette with an
   empty recents ring. SHA `5f25eea`.

## Gate (once, end of batch)
- `npx tsc --noEmit` (whole web project): clean, exit 0.
- `npm test`: **355 passed / 0 failed** (322 baseline + 33 new).
- ESLint: NOT configured in this repo — `next lint` drops into an interactive
  setup prompt, so the established web gate is tsc + npm test (matches prior
  ticks' gate lines). `make lint` / `make test` target the Python suite, which
  these frontend-only changes don't touch.

## Push
`046acf1..5f25eea  main -> main`, verified on origin/main via git fetch.

## Notes for next tick
- Frontend override still active. F49-F60 batch now has 7 open: F49 (shot-detail
  prev/next via recents ring + [/] keys), F53 (stat-card delta chips), F54
  (confidence-trend sparkline — long-open F16/F27/F41, all prereqs now in), F55
  (/shots scroll-to-top pill), F56 (per-class swatch legend under ingest-tempo),
  F57 (g s / g h / g u go-to chords), F60 (/stats class tiles carry active
  window as deep-link). Roadmap healthy (>5 open), no refill needed.
- F52's shotsFilterParts is now the shared filter-describe primitive (toast +
  tab title both consume it) — extend it there if a third surface needs the same
  phrasing.
- `relativeTime(then, now)` is a reusable general helper now; the settings pages
  that hand-roll "ago" math (audit / sessions / trust) could consolidate onto it
  in a future tidy-up slice.
