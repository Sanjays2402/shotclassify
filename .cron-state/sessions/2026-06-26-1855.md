# Tick 36 session -- 2026-06-26 18:55 PT (Cake, cron)

FRONTEND OVERRIDE active (set 2026-06-23). 5 frontend slices shipped to main.

## Shipped (one commit each)
1. **F86** `98b989b` -- shared export-format catalogue. `EXPORT_FORMATS` +
   `exportFormatByKey()` in lib/shot-export ({key, noun, short}). CopyExportButtons
   (full noun) and BulkExportButtons (compact `short`) both render by mapping the
   catalogue so the single-shot + bulk export trios can't drift; per-surface
   presentation (icons/tooltips/serializer dispatch) stays local. 4 tests.
2. **F82** `ccdc380` -- expand all / collapse all for the shot-detail rail.
   lib/detail-rail gained pure `collapseAll()`/`expandAll()`/`allCollapsed()`/
   `allExpanded()`; the page renders a mounted-gated header button offering the
   action that does something. 6 tests.
3. **F83** `b21b26d` -- recently-viewed count badge on the palette Shots nav row.
   Pure `recentCountLabel(href, count)` + `RECENT_BADGE_ROUTE` ("N recent" only
   for /shots with a positive finite count); component reads `recents.length`.
   3 tests.
4. **F88** `5bfebf7` -- filter breadcrumb on the notifications inbox. New pure
   `lib/notif-filter-chips` (Search/Kind/Unread chips, kind-label resolution +
   fallback) + `<NotifFilterBreadcrumb>` mirroring the shots breadcrumb; page
   wires `clearOneFilter()` reading the debounced query. 9 tests. (`/webhooks`
   half re-tracked as F92.)
5. **F81** `d75ef82` -- `[ ]` keyboard hint beside the ShotNav position counter.
   Hidden < sm, aria-hidden. Pure render; no lib change.

## Gate (once, end of batch)
- `npx tsc --noEmit` (whole web project): clean.
- `npx tsx --test --test-force-exit lib/*.test.mts`: 480 passed / 0 failed
  (458 baseline + 22 new). Needs --test-force-exit (known dangling-handle suite).
- `npx next build`: compiled successfully. /shots, /stats, /notifications still
  prerender static; /shots/[id] dynamic. ZERO Python touched.

## Notes / carry-forward
- Repo-wide `ruff check` still reports ~536 PRE-EXISTING errors from ruff-version
  drift. Untouched this tick (no Python). Flagged for Sanjay; needs a separate
  `ruff check --fix` + pin bump, out of scope for the frontend override.
- F85 partially mooted: the /stats window buttons already carry "(press W to
  cycle)" in their title tooltip; remaining work is a VISIBLE faint hint.
- Backlog refilled F91-F96 (count-active-filters badge, /webhooks breadcrumb,
  rail expand/collapse chord, per-row export menu, Inbox unread badge, section
  affordance copy unification).
