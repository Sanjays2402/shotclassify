# Tick 41 session — 2026-06-27 ~14:00 PT (Cake, cron)

FRONTEND OVERRIDE active. Shipped 5 frontend slices, gated once, pushed to main.

## Features (5)
1. **F112** — shared "N of M <noun>" count-label helper. New pure `lib/count-label`
   `ofTotalLabel(shown, total, {prefix, singular, plural, onlyWhenNarrowed})` is the
   single source of truth; `deliveryFilterCountLabel` delegates to it byte-identical
   (15 webhook tests stay green) and `/notifications` reads its "N of M match" line
   off it. 10 new tests. (8d6d818)
2. **F98** — canonical `<EmptyState>` on `/digest` (Sparkle + "Try the demo") and
   `/admin/seats` (bare variant, Users + "Invite teammates"). Component-level. (f0a97fb)
3. **F116** — webhooks status-legend swatch a11y. New pure `statusSwatchAria` builds
   an explicit action-oriented aria-label + title; decorative spans hidden from AT so
   the explicit name wins; cue-yellow focus-visible ring. 5 tests appended to
   webhook-delivery-chips.test (25->30). (3de56af)
4. **F84 (flagship)** — per-row inline preview drawer on `/shots`. ID-column chevron
   expands a drawer under the row with OCR snippet + mini conf bars + rationale. New
   pure `lib/shot-preview` view-model (14 tests) + `<ShotPreviewDrawer>` presentational
   renderer + connected `<ShotPreviewRow>` (lazy SWR, only mounts on expand, reuses the
   detail endpoint so a visited shot is cache-served). Closes F11/F28/F72/F84. (55162da)
5. **F115** — visible DotsThreeOutline glyph + hover title on the `/shots` export-column
   header (was an empty `<th aria-label>`). Pure render. (245fa2e)

## Gate (once, end of batch)
- `npx tsc --noEmit` clean (whole web project).
- `npx tsx --test --test-force-exit lib/*.test.mts` — **597 passed / 0 failed**
  (568 baseline + 29 new: count-label 10, statusSwatchAria 5, shot-preview 14).
- `npx next build` — compiled successfully in 5.6s, 62/62 static pages. `/shots` and
  `/webhooks` still prerender static; `/shots/[id]` dynamic. The F84 drawer's lazy
  fetch lives in a child that only mounts on expand, so it did NOT force `/shots`
  dynamic.
- ZERO Python touched — all web/ TS/TSX — pytest baseline cannot regress.

## Notes / lessons
- Lib import convention: non-test lib SOURCE files import siblings with NO extension
  (`from "./count-label"`); only `*.test.mts` files use the `.ts` extension. The patch
  tool's LSP flagged a stale `.ts` import mid-edit — corrected before gating.
- Every test addition to a pre-existing `*.test.mts` was done via `patch` (append),
  never `write_file`; only the two brand-new test files used `write_file`.
- Backlog refilled with F117-F128 (12 fresh, unblocked, non-duplicate items). Several
  F84 follow-ons (grid drawer, `o` keyboard, copy-OCR, expand-all). F125 is the one
  allowed backend slice — the per-hour mean-conf field that finally unblocks the
  5x-tracked /stats confidence sparkline.

## Roadmap status
221 tracked, 197 complete. Frontend override still active.
