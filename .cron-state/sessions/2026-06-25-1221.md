# Tick 30 — 2026-06-25 12:21 PT (Cake, cron)

FRONTEND OVERRIDE active. Shipped 5 web/ slices, gated once, pushed to main.

## Shipped (5 commits on main)
1. b434c97 feat(web): theme-aware delta / zero-line chart tokens (F31)
   - lib/chart-theme.ts: positiveStroke/negativeStroke/positiveFill/negativeFill/
     zeroLine for both themes (dim brightened so strokes lift off dark panels) +
     deltaStroke()/deltaFill() helpers (zero=positive/flat, NaN=neutral). +4 tests.
2. 82bd63b feat(web): explainer popovers on the stats KPI cards (F34)
   - lib/stat-explainers.ts (copy + windowLabel/scopeNote math) + <StatInfoPopover>
     "?" affordance on each of the 4 KPI cards. CategoryLegendChip popover pattern
     (hover + focus, Escape, close-delay). +7 tests.
3. fe35f48 feat(web): recently-viewed shots in the command palette (F32)
   - lib/recent-shots.ts MRU ring (push/dedupe/cap/parse, no-throw browser
     wrappers). Detail page records real visits; palette shows "Recently viewed"
     above search when query is empty, keyboard-navigable inline. +10 tests.
4. 83584eb feat(web): 'v' keyboard shortcut to cycle the shots view (F36)
   - Added cycle-view to SHORTCUTS (scope shots) -> shows in ? overlay's "On the
     shots list" section automatically. Page listener input-guarded + modifier-safe,
     persists via the view-mode storage key. +1 test.
5. 5c4cefe feat(web): shots list reads deep-link query params on load (F30)
   - lib/shots-deeplink.ts validates category/q/tag/min_conf/since/until/sort/pinned,
     applied once on mount under a Suspense boundary, then clears the query string.
     Stats chips + legend popovers + pinned quick-bar deep-links now land
     pre-filtered (verified they already emit ?category= / ?pinned=true). +13 tests.

## Gate (once, whole batch)
- tsc --noEmit: clean (whole web project)
- npm test: 291 passed / 0 failed (256 baseline + 35 new)
- next build: compiled successfully in 7.8s; /shots still prerenders STATIC,
  confirming the new Suspense boundary correctly contains useSearchParams.
- Zero Python touched -> pytest baseline cannot regress. `next lint` not
  configured in this repo (no eslintrc); tsc + tests + build are the web gates.

## Notes / decisions
- 4 new lib modules, all DOM-free + unit-tested, each wired into a real surface.
- F31 deliberately lays the token groundwork the still-open confidence-trend
  sparkline (F41/F27/F16) will consume next.
- F30's parser accepts both whole-percent and 0..1-fraction min_conf so links
  built from either convention land correctly.
- recent-shots only records REAL records, never the seeded 404 sample, so the
  palette can't link to a ghost id.

## Backlog
- Marked done: F30, F31, F32, F34, F36.
- Refilled: F41-F48 (8 new), several building directly on this tick's primitives
  (sparkline on F31 tokens; share-link as inverse of F30; clear-recent / prev-next
  on the F32 ring; delta-chips on F31 tokens).
- Still open: F6, F9, F11, F16, F20-F23, F25, F26-F29, F33, F35, F37-F48.
