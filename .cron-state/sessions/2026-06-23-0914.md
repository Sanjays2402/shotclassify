# Tick 23 session log

**Time:** 2026-06-23 08:44 PT (lock acquired) -> 09:14 PT (push complete + state updated)
**Branch:** main
**Commits pushed:** 5 (76978ee, ccdf146, 53c30c5, f0d68d4, e6bfd9c)

## Features shipped

1. **76978ee** `feat(extract/receipt): warranty / return-period notice extraction into ReceiptFields.warranty`
   - 52 new tests; new `ReceiptFields.warranty` dict with `{kind, duration_days, notice}`
   - 3 kinds: return / warranty / no_returns
   - Duration normalisation day/week/month/year -> 1/7/30/365
   - LLM wire-format updated; enrich_receipt backfill list updated
2. **ccdf146** `feat(extract/document): page-number footer detection into DocumentFields.page_info`
   - 48 new tests; new `DocumentFields.page_info` dict with `{current, total, label, continued}`
   - NEW module: extract/document.py with `enrich_document()`
   - Pipeline.enrich now routes Category.document through enrich_document
   - 11 recognised shape families: Page N of M / Slide N / Sheet N / Pg. N / p. N / - N - / bare slash form
3. **53c30c5** `feat(redact): VIN (Vehicle Identification Number) redaction mode`
   - 32 new tests; new `vin` mode in PII_REDACT_MODES
   - ISO 3779 character set (no I/O/Q); at-least-one-digit-and-letter rule
   - Both labelled (`VIN: ...`) and bare 17-char runs captured
   - Whole-match collapse to `[REDACTED:vin]` for safety
4. **f0d68d4** `feat(extract/code): dead-import detection into CodeFields.unused_imports`
   - 55 new tests; new `CodeFields.unused_imports` list[str]
   - 4-language coverage (Python / JS / TS / JVM)
   - Per-shape identifier-check derivation (alias / symbol / top-level / class)
   - Data + shell languages return [] unconditionally
5. **e6bfd9c** `feat(extract/code): per-function cyclomatic complexity into CodeFields.complexity`
   - 47 new tests; new `CodeFields.complexity` list[{name, complexity}]
   - McCabe-style score per function: base 1 + 1 per decision point
   - 7-language coverage (Python / JS / TS / Java / Kotlin / Scala / C# / Go / Rust)
   - String-literal-aware brace matching for C-family body extraction
   - `else if` lookbehind on bare \\bif\\b matcher prevents double-counting

## Gate

- `uv run ruff check .` -> 536 errors (baseline; no new)
- `uv run pytest` -> 5586 passed, 3 skipped, 0 failed in 152.82s
- Fixups folded before push (6 errors): 4 N802 + 1 W291 + 1 E501

## Notable

- One bug caught by my own tests in the first gate run: the bare `\\bif\\b`
  matcher in the JS/Java/Go/Rust/Kotlin complexity counters was double-counting
  the `if` inside `else if`. Two tests (`test_python_high_complexity_function`
  and `test_real_go_handler`) had expectations off by one because of this.
  Fixed by adding `(?<!else\\s)` negative lookbehind on the bare `if` pattern
  and re-running. Always trust the tests over the manual count.

## Roadmap status

- 132 tracked, **115 complete** (was 110)
- Backlog: 24 open items (was 23)
- 5 new items added: 145..149 (receipt delivery-eta, color-hex extractor,
  chat link-preview, complexity-outlier flag, phone redact upgrade)
