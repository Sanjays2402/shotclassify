# Tick 37 — 2026-06-26 23:34 PT (Cake, cron)

FRONTEND OVERRIDE active. Shipped 5 frontend slices + 1 coverage-fix commit, all to `main`.

## Shipped
1. **F92** (0ff0507) — `/webhooks` Recent-deliveries status + event filter with a
   removable breadcrumb. New pure `lib/webhook-delivery-chips.ts`
   (filterDeliveries / activeDeliveryChips / distinctDeliveryEvents; 14 tests) +
   `<WebhookDeliveryBreadcrumb>`. Event options derived from the data so a new
   subscribed event shows automatically; empty-filter result has a 1-click clear.
   Finishes the shots(F24)/notifications(F88)/webhooks breadcrumb-consolidation arc.
2. **F91** (4c288c4) — compact felt-green "N filters" pill on the `/shots` toolbar;
   click clears all. New pure `filterCountLabel()` in lib/filter-summary.
3. **F95** (7e6fc5c) — palette Inbox-row "N unread" badge. Pure `inboxCountLabel()`
   (off-route/zero/invalid -> null, 99+ cap); palette best-effort fetches unread on
   open, hides badge on any error. 4 tests.
4. **F93** (0d5d703) — Shift+E / Shift+C expand/collapse-all rail chords. Pure
   `railChordAction()` in lib/detail-rail (requires Shift, forbids Cmd/Ctrl/Alt,
   ignores bare letters). HotKeys bare-letter nav + sequence tracker now skip
   Shift-held keys so Shift+C never also routes to /calibration nor completes `g c`.
   Detail-scope SHORTCUTS entries + on-page kbd hint. 7 tests.
5. **F85** (5246779) — visible "W" kbd hint by the `/stats` window buttons so the
   F79 cycle is discoverable on-page. Pure render.

## Coverage fix (caught mid-tick)
6. **8945541** — the F91 commit's `write_file` had OVERWRITTEN the pre-existing
   `lib/filter-summary.test.mts` (12 tests) with a 7-test rewrite, silently dropping
   coverage. Restored the 12 originals + kept the 3 new filterCountLabel tests (15
   total). The patch tool even flagged a "sibling subagent" touching the file — that
   warning WAS the clobber; treat it as a real signal next time.

## Gate (once, whole batch)
- `npx tsc --noEmit`: clean.
- `npx tsx --test --test-force-exit lib/*.test.mts`: **508 passed / 0 failed**
  (480 baseline + 28 net new: F92=14, F91=3-on-top-of-restored-12, F95=4, F93=7;
  F85 pure render).
- `npx next build`: compiled successfully in 5.6s; /shots, /stats, /webhooks static,
  /shots/[id] dynamic. ZERO Python touched.

## Lessons / notes for next tick
- **When a lib already has a `*.test.mts`, APPEND via `patch` — never `write_file`
  the whole file.** Lost-then-restored 5 tests this tick because of that.
- The generic `matchesShortcut` bare-combo path is built around shifted *glyphs*
  (like `?`), so it CANNOT reliably reject Shift+letter. For Shift+letter chords use
  a dedicated explicit-shift helper (as F93 did with railChordAction).
- Backlog refilled with F97-F108 (12 new). ~40 frontend items still open.
- Pre-existing repo-wide ruff drift (~536 errors) still flagged for Sanjay; untouched.
