# Tick 15 — 2026-06-22 03:53 PT

Cake (cron), 20-min batch tick. Shipped 5 features straight to main.

## Features

1. **f451683** `feat(extract): cross-category currency-amount extractor into raw["amounts"]`
   - 50 new tests; ISO 4217 codes across 40+ currencies; 4 shapes (symbol-prefix / symbol-suffix / ISO-prefix / ISO-suffix); decimal normalisation handles US, EU, French conventions.

2. **04ecfde** `feat(extract): cross-category postal-code extractor into raw["postal_codes"]`
   - 49 new tests; 10 countries (US/UK/CA/DE/FR/NL/AU/JP/IN/BR); self-anchored vs anchored shapes; Canadian first-letter restriction; French departement-0 rejection.

3. **2476f8a** `feat(extract/code): regex-literal extraction into CodeFields.regexes`
   - 47 new tests; 8 flavors (js/python/ruby/perl/go/java/rust/c#); per-delimiter-pair Ruby/Perl matchers; JS division-vs-regex disambiguation.

4. **bb9a7c6** `feat(extract/chat): edited-message marker detection into ChatFields.edits`
   - 26 new tests; (edited) / (edited 2m) / [edited] / "edited at 12:34" markers across iMessage/Slack/Discord/WhatsApp/Telegram; substring-defence on unedited/credited.

5. **4f71f06** `feat(extract/chat): per-message emoji reaction counts into ChatFields.reactions`
   - 30 new tests; Slack shortcode form / Discord inline Unicode / iMessage reaction-by-name; per-line >=30% emoji density heuristic.

## Gate

- ruff: 536 → 536 (no new errors; 5 I001 fixups folded via --fixup + --autosquash before push)
- pytest: 3402 passed / 3 skipped in 140.03s (+202 over tick 14's 3200 baseline; 50 + 49 + 47 + 26 + 30 = 202)
- pushed to origin/main; verified `git log --oneline origin/main` shows all 5 commits

## Notes

- **Disk-space friction:** initial __init__.py write hit "No space left on device" because /var/folders had 116MiB free — leftover pytest-of-sanjay tmpdirs from previous runs had filled it. Freed 620MiB by removing pytest-of-sanjay/ + go-build cache + node-compile-cache before continuing. This is the second tick (after #13) with disk pressure; might be worth a permanent `tmp_path_retention_policy = "failed"` in pytest.ini if it recurs.
- **Slice separation tactic:** features 4 + 5 both touched chat.py / schemas.py / client.py. Used a `cp` stash to /tmp, stripped the reactions code out before committing edits, then restored from stash for reactions — kept the slices clean as separate revertible commits without resorting to `git add -p` in a non-interactive cron.
- **Roadmap refilled** with items 98..102 (receipt line-item modifiers, code secret/key sniffing, cross-category emoji-density tally, GraphQL error extraction, chart data-table fallback). Backlog stays at 23 open.
