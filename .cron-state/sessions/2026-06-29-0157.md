# Tick 49 — 2026-06-29 01:57 PDT

5 frontend slices, all on main, gate green.

1. F65 (a1c2249) — stats Mean-confidence KPI now appends a signed pts trend delta from per-hour mean_confidence (first vs last populated bucket). lib/conf-trend.ts, 6 tests.
2. F147 (fb2ef96) — /webhooks failed delivery rows get an inline Retry (action=redeliver, per-row spinner, refresh + toast). lib/delivery-retry.ts, 5 tests.
3. F153 (d5b5141) — /digest recipient validated client-side; blank = DIGEST_TO, malformed disables Send + aria alert. lib/digest-recipient.ts, 4 tests.
4. F154 (d108d77) — /digest by-category rows get faint share-of-total bars (tiny class floors to 4% sliver). lib/category-share.ts, 5 tests.
5. F158 (f122f6c) — /digest per-day strip gets busiest-day accent + "Busiest <date> N shots M/day avg" caption. lib/digest-peak.ts, 4 tests.

Gate: 836 web tests pass, tsc clean, next build OK. Only web/ touched. Caught a self-inflicted "0.0%" vs "0%" test fail on slice 4 and fixed before pushing.
