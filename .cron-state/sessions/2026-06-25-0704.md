# Tick 29 session — 2026-06-25 07:04 PT (Cake, cron)

**Mode:** 20-min batch shipping loop, FRONTEND OVERRIDE active (set 2026-06-23).
**Branch:** main (committed direct, pushed to origin/main: 66d6987..df45f5b).
**Result:** 5 frontend slices shipped from the F-backlog. All gates green.

## Shipped (5 commits)
1. `6af3bbe` feat(web): removable filter-summary breadcrumb on the shots table (F24)
2. `d571bb6` feat(web): dark-mode-aware recharts theming across all chart surfaces (F19)
3. `31a4a45` feat(web): table / grid / compact view toggle on the shots list (F10)
4. `9e243c3` feat(web): pinned-shots quick-bar on the Live page (F15)
5. `df45f5b` feat(web): per-category legend hover popover on the stats class mix (F14)

## What each slice did
- **F24 (filter breadcrumb):** `lib/filter-summary.ts` -- `activeFilterChips()` builds an
  ordered, individually-removable chip list (class/search/tag/conf/dates/pinned),
  `clearFilter()`/`clearAllFilters()` return inert-default copies; 0% conf floor is
  no-op, long searches truncate, category -> long label. `<FilterBreadcrumb>` thin
  renderer; wired into `/shots` with a `clearOne(key)` dispatcher; also DRY'd the two
  inline reset handlers onto one `resetAllFilters()`. 12 tests.
- **F19 (chart theming):** real dim-mode bug -- every recharts surface hard-coded
  near-black `rgba(11,15,12,...)` strokes invisible on dim panels. `lib/chart-theme.ts`
  light+dim token sets (axis/grid/tick/cursor/reference) + theme-stable broadcast
  tooltip; `useChartTheme()` hook resolves from the live `data-theme` attr via a
  scoped MutationObserver (re-themes live on toggle). Wired into `/shots/[id]`,
  `/calibration`, `/stats` (5 charts total). 6 tests.
- **F10 (view toggle):** `lib/view-mode.ts` table/grid/compact parse/cycle/label +
  isTabular/isCompact, persisted to localStorage; `<ShotGrid>` card presenter keeps
  bulk-select/pin/compare as overlay controls; `.tbl-compact` density CSS. 8 tests.
- **F15 (pinned quick-bar):** `lib/pinned-bar.ts` -- `pinnedQuickItems()` filters
  pinned, de-dupes, sorts newest-first, never-blank name, caps (default 12);
  `pinnedOverflow()` for "+N more". `<PinnedQuickBar>` self-fetches `pinned=true`
  history, returns null when empty (so seeded-sample never shows it). On `/`. 9 tests.
- **F14 (legend popover):** `lib/category-legend.ts` -- `categoryLegendSummary()`
  derives share (0% not NaN), clamps mean conf, builds `/shots?category=` link;
  `<CategoryLegendChip>` hover+focus popover (count/share/mean-conf/share-bar/link),
  Escape-closes, short close delay so the link is reachable. On the `/stats` class
  mix list. 8 tests.

## Gates
- `tsc --noEmit` -> clean (whole web project), re-run after every slice + at batch end.
- `npm test` -> **256 passed / 0 failed** (213 baseline + 43 new across 5 lib modules).
- All work is `web/` TypeScript only -- **zero Python touched**, so the ~6272-test
  pytest baseline cannot regress (didn't re-run it; nothing under packages/ or
  services/ changed). Confirmed via `git diff --name-only`.

## Notes / gotchas for next tick
- `next lint` is NOT configured in this repo (no `.eslintrc*` / `eslint.config.*`).
  Running it drops into the interactive "How would you like to configure ESLint?"
  setup prompt -- it is NOT a usable gate here. The reliable web gates are
  `npx tsc --noEmit` + `npm test` (lib/*.test.mts via node:test). Both green.
- `next build` still reproducibly stalls at worker-bootstrap on this host (Node 25 +
  Next 15.5) per tick 28 -- did not attempt it; tsc + tests are the trusted gates.
- F12 (confidence histogram on /stats) was already shipped as the "Confidence
  calibration" chart -- marked done, NOT padded with fake work. Shipped 5 genuine.
- Pattern reused every slice: pure `lib/*.ts` + `.test.mts` (node:test, no DOM) +
  thin `.tsx` renderer. 5 new test files, 43 new assertions.
- Deep-link gap surfaced (logged as F30): `/shots` ignores URL query params, so the
  new `/shots?pinned=true` + existing `/shots?category=` links land unfiltered.
  Wiring `useSearchParams` -> initial filter state is the obvious next pickup.
- Backlog refilled F26-F40 (15 items); F9/F11/F16/F20-F23/F25 still open.
