# tick 50 — 2026-06-29 04:21 PDT

Frontend-override batch. 4 solid features, gated once, pushed to main.

1. **678e7de** — compactNumber lib (12.3K/1.2M) wired into /stats KPI tiles
   (lifetime, corrections) + nav quota badge; exact grouped figure stays in
   title/aria so nothing's hidden. lib/num-compact.ts + tests.
2. **68eda78** — /shots preview drawer highlights OCR search matches via
   markMatches (case-insensitive, overlap-free, 200-seg cap), cue-yellow
   <mark>; highlight threaded from qDebounced. lib/search-mark.ts + tests.
3. **8c50812** — F155: keys/[id] try-it empty-secret state links "Generate one
   above" back to /keys#key-name. lib/key-trial.ts + tests.
4. **7bbc97a** — F40: keys table hover shows per-key share of total fleet
   traffic (fleetTotalCalls + fleetShareLabel, <1% floor) beside fleet-peak.

Dropped a planned 5th (kbd-hint formatter) — duplicated existing
lib/shortcuts.ts renderKeys/isMac, so shipping it would be padding. 4 over 5.

Gate: tsc 0, 853 web tests pass, next build OK, only web/ touched.
