# Tick 34 — 2026-06-26 07:50 PT (Cake, cron)

**Mode:** FRONTEND OVERRIDE active (Sanjay's standing directive since 2026-06-23). All 5 slices frontend/UX in the Next.js `web/` dashboard.

## Shipped 5 features (F61, F68, F64, F62, F63)

1. **F61 — extend the `g <x>` chord namespace** (0f75c15)
   - GOTO_CHORDS + SHORTCUTS gain `g d` (Demo), `g w` (Webhooks), `g k` (API keys), `g i` (Inbox).
   - Routes all verified to exist. Second letters chosen to avoid the reserved `g t` (scroll-to-top) and each other.
   - Two new guard tests: no chord reuses `T`/claims `g t`; all second letters distinct (deterministic routing).
   - Catalogue-only — the sequence tracker + HotKeys already generalise.

2. **F68 — `g <x>` chord hints on the command-palette nav rows** (5578e8b)
   - Instead of duplicate rows, decorated the EXISTING "Jump to" rows: each row whose href has a chord shows its `g <x>` kbd glyphs (sm:+ only) instead of the raw route.
   - New pure `chordForRoute()` / `chordKeysForRoute()` reverse-index GOTO_CHORDS (single source of truth, defensive). Round-trip test proves the reverse map is a perfect inverse of `routeForChord`.

3. **F64 — bulk "Copy as CSV"** (adc08f3)
   - `toBulkCsv()` — RFC-4180 quoting (comma/quote/CR/LF → wrap + double interior quotes), CRLF records, stable exported `BULK_CSV_HEADERS`, bare `confidence_pct` number (spreadsheet-sortable), `"; "`-joined tags. Empty selection → header row alone.
   - Third "Copy CSV" button in the bulk bar; toast type widened to include CSV. 6 new tests incl. the comma-in-tag phantom-column guard.

4. **F62 — neighbour LABELS on the shot-detail prev/next chevrons** (e5091dc)
   - `ShotNeighbors` gains `prevLabel`/`nextLabel` from the frozen ring snapshot; pure `neighborLabel()` (stored label → 8-char short-id fallback → ellipsis at 18 chars).
   - ShotNav renders an inline truncated span (hidden < sm) and folds the neighbour name into aria-label/title. Updated the 3 existing deepEqual shape assertions + 3 new tests.

5. **F63 — grid-density keyboard cycle (`d`)** (6b84035)
   - `d` on /shots cycles Roomy→Default→Dense via pure `nextGridDensity()`; shots-scope SHORTCUTS entry hinted "Grid view only".
   - Handler is **view-guarded** (grid only, via ref) AND **chord-guarded**: because this same batch added `g d` (Demo), the handler swallows a `d` arriving within 1200ms of a `g` so `g d` navigates without also flipping density. 3 new tests.

## Deliberately skipped (honesty over padding)
- **F65** (confidence-trend sparkline): `/api/aggregate.hourly` is COUNT-ONLY — no per-bucket mean confidence. Needs a real backend field first; deferred rather than ship a misleading chart.
- **F66** (scroll-to-top FAB): ALREADY shipped globally via `<ScrollProgress>` in layout.tsx. Marked done; would have been filler.
- **F67** (per-class legend under tempo chart): the tempo chart is a SINGLE felt-green series (total/hour), not per-class — a class legend there would mislead. Left open with a re-scope note.

## Gate
- `web` suite: **420 pass / 0 fail** (was 402; +18 new tests across goto-chords / shot-export-bulk / shot-nav / grid-density).
- `tsc --noEmit`: **clean**.
- `next lint`: not runnable (repo never completed interactive ESLint setup — pre-existing; TS is covered by tsc).
- Python `ruff check`: 536 errors, **PRE-EXISTING** — proven byte-identical at HEAD~5 via a throwaway worktree. Cause: ruff-version drift (pyproject pins `>=0.6`, resolved binary is `0.15.15`, which enforces new UP/I/F rules). My batch touched ZERO Python (git diff confirms all 13 files under `web/`) and added zero new lint errors.

## Flag for Sanjay
The Python `ruff check` is red repo-wide (536 errors) purely from ruff-version drift — not from any feature work. Worth a one-off `uv run ruff check --fix .` (292 auto-fixable) + pinning ruff to a specific version in pyproject, done as its own cleanup commit outside the frontend override. Until then the Makefile `make lint` target will always report red regardless of what ships.

## Next tick
Roadmap refilled with F69–F80 (12 fresh frontend items). Strong unblocked picks: F72 (inline preview drawer — long-open F11/F28), F73/F74 (FilterBreadcrumb + EmptyState consolidation), F77 (collapsible detail rail), F80 (single-shot CSV reusing F64's quoting). Avoid F65/F67 until their backend/scoping notes are resolved.
