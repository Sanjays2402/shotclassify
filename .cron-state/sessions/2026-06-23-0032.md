# Tick 21 session log

**When**: 2026-06-23 00:32 PT
**Branch**: main (commit-direct, no PR)
**Goal**: Ship 5 feature slices per the batch loop.

## Features shipped (5/5)

| # | Slice | Commit | Tests added |
|---|-------|--------|-------------|
| 1 | Cross-category invoice ID extractor (raw["invoice_ids"]) | c569e9c | 77 |
| 2 | Receipt split-payment / multi-tender (ReceiptFields.tenders) | 93fdceb | 49 |
| 3 | Chat forwarded-message markers (ChatFields.forwards) | c4303eb | 54 |
| 4 | Code shell-script style detection (CodeFields.shell_style) | 9a7ef36 | 73 |
| 5 | Cross-category emoji tally (raw["emojis"]) | df872f0 | 57 |

**Total: 310 new tests** across the 5 slices.

## Gate

- `uv run ruff check .` -> 536 errors (BASELINE; no new errors)
- `uv run pytest` -> **5052 passed, 3 skipped, 0 failed** in 123.00s

## Fixups

- One I001 fixup folded via `--fixup` + `--autosquash` into the forwards
  commit (c4303eb): ruff wanted no blank line between `from __future__`
  and the next import paragraph in `test_chat_forwards.py`.

## Disk-space note

Hit `/var/folders` 100% full at the first pytest run. Freed 1.1Gi by
removing leftover `pytest-of-sanjay` tmpdirs (936MiB) plus the
`node-compile-cache` (24MiB) before the rerun. The leftover tmpdirs
have been an issue on multiple ticks now -- worth documenting in
shotclassify-20min-prompt.md as a known recurring cleanup.

## Notable design decisions

### 1. invoice_ids (raw["invoice_ids"])

Three-shape matching with span-claim defence: slash-form (`2024/INV/0099`)
runs FIRST so the inner "INV" isn't also consumed by the keyword-led
"Invoice 0099" matcher. Bare "Bill 12345" prose rejected because Bill
matcher REQUIRES the compound form (Bill No: / Bill Number: / Bill #) --
"Bill: $50" is too common on dinner receipts to be safe with a
bare-colon match. Short prefixes (Q/QU/CN/PO/AR) require 4+ char body
so "Q-1"/"PO-1" prose tail rejects; long prefixes
(INV/INVOICE/BILL/EST/QUOTE/CREDIT/PURCHASE) require 3+ char body so
3-digit small-business invoices (INV-001) still parse. Hash prefix is
stripped from canonical id because `#` is printer convention.

7 kinds: invoice / bill / quote / estimate / credit_note /
purchase_order / accounts_receivable.

Distinct from `receipt.order_number` (per-receipt primary) and
`raw["stripe_ids"]` (Stripe-prefixed cus_/inv_/etc).

### 2. tenders (ReceiptFields.tenders)

Per-line matching (keyword + amount must sit on the SAME line) so a
"Visa" header at the top doesn't pair with the total at the bottom.
Surfaces ONLY when 2+ distinct tender lines detected -- single-tender
receipts use the existing payment_method / tendered slots.

Multi-word forms beat short aliases via catalogue ordering:
- "American Express" -> amex
- "Master Card" -> mastercard
- "Gift Card" -> gift_card (NOT bare card)
- "Credit Card" -> credit
- "Cash App" -> cashapp

23 catalogued kinds: cards (visa/mastercard/amex/discover/jcb/diners/
unionpay), wallets (apple_pay/google_pay/samsung_pay), apps
(paypal/venmo/cashapp/zelle), cash family (cash/check/ebt),
stored-value (gift_card/store_credit), fallback (card/credit/debit).

Both US (1,234.56) and EU (1.234,56) decimal conventions parse. Negative
sign stripped (field semantic implies positive amount). Dedupe on
(kind, amount) pair.

LLM wire format in classify/client.py updated. enrich_receipt backfill
respects existing-list-wins.

### 3. forwards (ChatFields.forwards)

Priority-ordered with consumed-span defence so the more-specific
bracketed (`[Forwarded from #channel]`) and parenthesised
(`(Forwarded from Alice)`) shapes claim their regions BEFORE the bare
`Forwarded from <X>` matcher fires, preventing double-tagging.

Bare-Forwarded badge matcher requires FULL-LINE match (with optional
arrow / italic markers) so mid-sentence prose "I forwarded that
yesterday" never fires. Forward-arrow emoji alone (↪️ without the
Forwarded word) doesn't fire because the keyword is the discriminator.

"Forwarded many times" tagged as a distinct `forwarded_many` kind
because dashboards care about viral-propagation chain markers (often
misinformation in real-world WhatsApp).

3 kinds: forwarded / forwarded_many / shared.

Sender attribution from nearest preceding `Sender:` line. Dedupe on
(kind, forwarded_from, sender) tuple.

### 4. shell_style (CodeFields.shell_style)

Detection precedence -- tcsh/csh checked BEFORE fish because both use
`set` but tcsh's `set VAR = value` (with spaces around =) wins over
fish's broader `set VAR value` matcher.

PowerShell wins immediately when present because the cmdlet vocabulary
is highly distinctive.

Shell-language gate enforced: non-shell language returns None
unconditionally so a Python string containing `[[ $foo ]]` won't
false-positive. When language is None runs content sniffing returning
the matched style ONLY on a positive signal -- generic snippets
(JSON/YAML/prose) with no signals return None instead of posix to
avoid mis-classifying.

6 styles: bash / zsh / fish / powershell / tcsh / posix.

### 5. emojis (raw["emojis"])

ZWJ sequences combine two emoji codepoints into one logical unit so
family compounds (👨‍👩‍👧‍👦), technologist (👨‍💻), and rainbow-flag
(🏳️‍🌈) all surface as single entries with the full codepoint sequence
preserved (`U+1F468 U+200D U+1F469 U+200D ...`).

Skin-tone modifiers attach to preceding hand/face emoji so 👍🏻 light
and 👍🏿 dark count as DISTINCT entries (the modifier semantic is
meaningful).

Variation selectors (U+FE0E text vs U+FE0F emoji) combine with
preceding base char so ❤ bare and ❤️ with VS-16 are distinct.

Plain ASCII symbols ($/€/£/©/®/→) intentionally NOT captured because
they appear in non-emoji contexts (math, prose, copyright).

10 detected codepoint ranges covering all the major Unicode emoji
blocks including Geometric Shapes Extended (U+1F7E0..U+1F7FF) for
status indicators (🟢🟡🔴 etc).

Output sorted by descending count for "most-used-first" rendering.
Cap 50 distinct entries.

## Push

Verified `df872f0` is on `origin/main`:

```
df872f0 feat(extract): cross-category emoji tally extractor into raw["emojis"]
9a7ef36 feat(extract/code): shell-script style detection into CodeFields.shell_style
c4303eb feat(extract/chat): forwarded-message marker detection into ChatFields.forwards
93fdceb feat(extract/receipt): split-payment / multi-tender detection into ReceiptFields.tenders
c569e9c feat(extract): cross-category invoice/quote/PO ID extractor into raw["invoice_ids"]
6044eee chore(autoship): tick 20 session log and state update
```
