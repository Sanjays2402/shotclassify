# shotclassify autoship session — tick 19 (2026-06-22 17:43 PT)

Operator: Cake (cron) — 20-minute batch loop.

## Outcome

5 features shipped, all on `main`, push verified at `0bdcbba`.
Gate clean (ruff at baseline 536, 4495 / 3 skipped pytest).

## Features

1. **f4661f4** — `feat(extract/error): Apollo Client / Apollo Server error parsing (framework='apollo')`
   * 39 tests. Three shapes: bracketed `[GraphQLError: ...]`, top-level `ApolloError: ...`, typed Apollo Server exception classes. Anchor-required for typed exceptions to avoid colliding with form-library ValidationError. Placed AFTER GraphQL JSON branch, BEFORE python/node.

2. **bb6c60a** — `feat(extract): cross-category currency-pair extractor into raw["fx_pairs"]`
   * 63 tests. List of `{base, quote, rate}` dicts. 100-token curated catalogue (40 fiat ISO 4217 + ~60 top-by-market-cap crypto). Both-sides-in-catalogue safety. Pipeline writes for every category.

3. **c45a1bd** — `feat(extract/code): dependency version-pin extraction into CodeFields.dep_pins`
   * 66 tests. 8 ecosystems: npm/pip/cargo/gem/composer/go/maven/gradle. Per-shape pattern matching not language-tag-gated; composer before npm; cargo table before simple; go module-name dot-required; header-word blocklist.

4. **8fba774** — `feat(extract/receipt): refund-reason extraction into ReceiptFields.refund_reason`
   * 52 tests. Three priority tiers: compound `Refund Reason:` / `Void Reason:` / `Return Reason:`, bare `Reason:` (requires refund_amount anchor), inline `Refund - <reason>`. Cleaning rejects pure numbers, currency, status words.

5. **0bdcbba** — `feat(extract/receipt): line-item modifier / customisation extraction into ReceiptLine.modifiers`
   * 47 tests. 4 kinds: add/remove/sub/note. Sigil-prefix unconditional; word-prefix requires indentation; note kind for bare indented short text without price tail. Modifier-with-price-tail wins over base-item interpretation.

## Gate

* ruff: 536 (baseline). Two fixups folded via `--fixup` + `--autosquash` before push.
  * E501 line-too-long: wrapped maven dep catalogue comment across multiple lines in `code.py`.
  * N802 lowercase-test-name: renamed `test_typed_error_with_useQuery_anchor` → `test_typed_error_with_usequery_anchor`.
* pytest: 4495 passed / 3 skipped in 154.80s (up 267 from tick 18's 4228).

## State

* Roadmap: 117 features tracked, 95 done. Backlog at 23 open with 5 new items (118..122) added: Twilio SID extractor / chat polls / receipt ship_to address block / code dead-code markers / trading positions notation.
* Last 7 commits on `origin/main`:
  - 0bdcbba modifiers
  - 8fba774 refund_reason
  - c45a1bd dep_pins
  - bb6c60a fx_pairs
  - f4661f4 apollo
  - d76b18c chore(autoship): tick 18 session log and state update
  - 9c2824a todo_tickets (tick 18)

## Notes

* Disk space at 99% on `/var/folders/` at tick start — freed 854MiB by removing pytest-of-sanjay/ + go-build* + uv builds-v0 to make headroom for pytest tmp output. Same recurring issue as ticks 13 and 15.
* The Apollo parser's anchor-required defence for typed-server classes is the key safety property. Without it, every `ValidationError: ...` from joi/yup/zod/class-validator would mis-tag innocent JS captures.
* The `_VERSION_SPEC` regex in code.py is declared but unused (the per-ecosystem regexes use the embedded body directly). Left in place as a documentation reference for future contributors; could remove next tick if no follow-up uses it.
* Apollo `parse_apollo_error` is exposed in `__init__.py` for direct testing — mirrors how `parse_graphql_error` is exposed.
* No web UI / API changes this tick — pure Python extractor work.
