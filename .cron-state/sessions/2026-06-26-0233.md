# Tick 33 — 2026-06-26 02:33 PT (Cake, cron)

**Mode:** FRONTEND OVERRIDE active (Sanjay's directive since 2026-06-23). All 5 slices are web/ UX work; zero Python touched.

## Shipped (5 frontend slices)

1. **F57 — Linear-style "go to" chords** (`5ecec8a`)
   - `lib/goto-chords.ts`: pure chord->route map. `g l` Live, `g h` Shots, `g s` Stats, `g u` Upload, `g c` Calibration. `routeForChord` normalises case/whitespace, rejects `g t` (scroll-to-top, owned by HotKeys) + unknown chords, degrades non-strings to null.
   - `shortcuts.ts`: new "goto" scope + 5 chord entries so the shared `createSequenceTracker` recognises them.
   - `HotKeys.tsx`: resolve a completed chord BEFORE the legacy bare-letter switch.
   - `ShortcutsHelp.tsx`: "Jump to a section" group. 10 tests (incl. end-to-end through the real tracker).
   - Fills the genuine gap that /stats had no keyboard jump.

2. **F49 — shot-detail prev/next over the recently-viewed ring** (`aded544`)
   - `lib/shot-nav.ts`: pure neighbour math over the newest-first MRU ring. prev=newer (left), next=older (right). Shot absent from ring -> index -1 -> nav hides.
   - `components/ShotNav.tsx`: chevrons + `[`/`]` keys. **Snapshots the ring ONCE on mount** — React fires child effects before parent effects, so this child captures the ring BEFORE the page's `recordRecentShot()` bumps the current shot to front, keeping neighbours stable.
   - `shortcuts.ts`: `[`/`]` fill the previously-empty "detail" scope. 9 tests.

3. **F60 — /stats class tiles carry the active window into /shots** (`97b16b3`)
   - `lib/stats-class-link.ts`: `sinceForWindow(hours, now)` = UTC date `hours` back (date-granular, matching the /shots `since` filter). `statsClassLink` composes category+since via F47's `buildShotsDeepLink`.
   - `stats/page.tsx`: capture `now` on mount; grid keeps the bare class link pre-mount (SSR parity), upgrades to the windowed deep-link after. 7 tests incl. a round-trip back through the F30 parser.

4. **F29 — column-density control for the /shots grid** (`65a672b`)
   - `lib/grid-density.ts`: parse/serialize + no-throw storage mirroring view-mode/stats-window. `gridColumnsClass()` returns FULL STATIC Tailwind strings (roomy 1/2/2/3, default 1/2/3/4 = original, dense 2/3/4/6) — not interpolated, so the compiler emits them.
   - `ShotGrid.tsx`: `density` prop (default = no behaviour change). `shots/page.tsx`: persisted state + a segmented toggle that appears only in grid view. 11 tests.

5. **F35 — bulk "copy as JSON / Markdown" on the multi-select** (`d0b122a`)
   - `lib/shot-export-bulk.ts`: `toBulkJson` (array of per-shot export objects, reusing F18's `toExportObject`) + `toBulkMarkdown` (ONE compact summary table with pipe/newline escaping) + `bulkExportToastMessage` (honest about cross-page copied-vs-selected split).
   - `components/BulkExportButtons.tsx`: in the bulk-actions bar, same non-secure-context clipboard fallback as CopyExportButtons. `shots/page.tsx`: memoised export rows from selected ids on the current page. 11 tests.

## Gate (once, end of batch)
- `tsc --noEmit`: clean (whole web project)
- `npm test`: **402 passed / 0 failed** (355 baseline + 47 new across 5 new lib modules)
- `next build`: compiled successfully in 13.5s; **/shots AND /stats both still prerender static** — new density Tailwind classes emit, Suspense boundary intact.
- `next lint` is NOT configured in this repo; tsc + tests + production build are the reliable web gates. All green.
- Pushed `a835405..d0b122a` to origin/main; verified.

## Notes / learnings
- Full `npm test` ran ~43s under machine contention this tick (baseline ~13s); ran it in the background with notify rather than blocking a foreground 300s window. Signal was clean.
- Child-before-parent effect ordering is the key correctness trick in ShotNav (F49) — it lets the nav freeze a pre-visit ring without the page needing to pass it down.
- Tailwind v4 only emits classes it sees literally; the grid-density lib keeps every column combo as a full static string for exactly this reason (verified by the production build keeping /shots static + rendering all densities).

## Backlog
- Marked done: F29, F35, F49, F57, F60.
- Refilled F61-F68 (extend chord namespace, ShotNav labels, grid-density keyboard cycle, bulk CSV export, conf-trend sparkline, scroll-to-top, stats swatch legend, palette go-to rows).
- Still open from earlier: F6, F9, F11, F16, F20-F23, F25, F26-F28, F33, F38-F41, F43, F46, F48, F53-F56.
