# Tick 40 session — 2026-06-27 09:16 PT

Cake (cron), 20-min frontend batch loop. FRONTEND OVERRIDE active.

## Shipped (5 slices, all on origin/main e1b7b90..492d4e7)

1. **F109** (d83b4d3) — per-row Copy-as export menu reaches the GRID view.
   Added RowExportMenu to the ShotGrid card footer beside Compare. Compact
   already had it (compact reuses the table markup via isTabular). New pure
   `shotRowToExportInput()` in lib/shot-export is the single list-row->export
   mapper (optionals -> null/[]); the table page's inline object was replaced
   with it so all three layouts feed identical data. 5 tests.
2. **F114** (5edfd12) — arrow-key navigation in the per-row export menu.
   Up/Down/Home/End roving nav (open primes first item, Arrows wrap, Enter/
   Space fire natively, Escape closes); roving tabindex so Tab leaves cleanly.
   New pure lib/roving-index (rovingIndex + isRovingKey). 10 tests.
3. **F111** (46df86a) — partial-fold count badge on the shot-detail rail.
   Faint "(N folded)" beside Expand/Collapse-all when partially folded. New
   pure collapsedCount() + foldedCountLabel() (null at zero AND at all-folded;
   counts known slots only). 5 tests.
4. **F110** (ba55424) — "N seen" breadth affordance on the webhooks event
   filter. Live distinct-event count beside the Event label. New pure
   distinctEventCountLabel(). 3 tests.
5. **F113** (492d4e7) — Copy-link button on the webhooks deliveries view.
   Mirrors shots CopyViewLinkButton; disabled until a filter is active. New
   pure buildDeliveryDeepLink() + deliveryLinkToastMessage() reusing the F103
   builder/validators. New CopyDeliveryLinkButton component. 4 tests.

## Gate (once, end of batch)
- `npx tsc --noEmit` — clean (whole web project).
- `npx tsx --test --test-force-exit lib/*.test.mts` — 568 passed / 0 failed
  (543 baseline at tick 39 + 25 net new).
- `npx next build` — compiled in 5.1s; /shots + /webhooks static, /shots/[id]
  dynamic; 62 static pages.
- ZERO Python touched -> pytest baseline cannot regress.

## Notes / lessons
- ALL test additions to pre-existing *.test.mts were `patch` (append), never
  write_file (heeded tick-37 clobber lesson). Only the brand-new
  roving-index.test.mts used write_file. Tooling flagged "sibling subagent"
  touched two test files mid-tick; verified standalone — nothing clobbered,
  counts only went up.
- F112 (shared N-of-M helper) left OPEN with a note: deliveryFilterCountLabel
  (F102) is the closest shape but a true shared helper needs a noun/verb-
  configurable signature; skipped to keep the batch focused, not filler.
- Backlog has F112 + F115 + F116 fresh + ~38 older; above the <5 refill
  threshold -> NO refill this tick. Next candidates: F115, F116, F112.
- Carried: ~536 pre-existing repo-wide ruff errors (version drift, Python),
  untouched by this batch. Still flagged for Sanjay.
