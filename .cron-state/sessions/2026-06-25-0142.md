# Tick 28 session — 2026-06-25 01:42 PT (Cake, cron)

**Mode:** 20-min batch shipping loop, FRONTEND OVERRIDE active (set 2026-06-23).
**Branch:** main (committed direct, pushed to origin/main).
**Result:** 5 frontend slices shipped from the F6-F25 backlog. All gates green.

## Shipped (5 commits)
1. `69fa65f` feat(web): app-wide toast primitive replacing per-page flash banners (F8)
2. `feae3de` feat(web): Skeleton loader primitive replacing ad-hoc animate-pulse rows (F7)
3. `240394a` feat(web): copy shot as JSON / Markdown on the detail page (F18)
4. `2558164` feat(web): command-palette facets for class / confidence / tag (F13)
5. `6566356` feat(web): what's-new changelog popover on the header version pill (F17)

## What each slice did
- **F8 (toast):** `lib/toast-store.ts` — framework-free external store via
  `createToastStore(scheduler)`; imperative `toast.success/error/info`; newest-first,
  capped, per-kind durations, sticky duration 0, stable getSnapshot for
  `useSyncExternalStore`. `components/Toaster.tsx` single mount in layout. Migrated
  `/shots` bulk + pin off `bulkFlash`. 11 tests (fake-clock auto-dismiss).
- **F7 (skeleton):** `lib/skeleton.ts` pure geometry + seeded xorshift ragged widths
  (no hydration drift). `<Skeleton>/<SkeletonText>/<SkeletonRows>`, 5 variants. One
  `.sc-skeleton` shimmer class with dim-mode + reduced-motion. Wired `/shots` +
  `/webhooks`. 8 tests.
- **F18 (export):** `lib/shot-export.ts` JSON + Markdown serializers (clamp, sort,
  pipe/newline escape). `<CopyExportButtons>` beside ShareActions, toast feedback,
  works on seeded sample. 9 tests.
- **F13 (palette facets):** `lib/palette-facets.ts` parses `class:`/`category:`/`in:`,
  `tag:`/`#`, `conf:`, `>/>=/</<= NN%`; residual -> q. CommandPalette filters
  `/api/history` + "Filtering" pill row. 14 tests.
- **F17 (changelog):** `lib/changelog.ts` feed + seen-pointer helpers. `<WhatsNew>`
  replaces static `v0.1` pill; unread dot; auto-open once per bump for returning
  users only. 9 tests.

## Gates
- `ruff check .` → 536 errors = documented baseline (zero new; all work is web/ TS).
- `pytest -q` → **6272 passed, 0 failed** (610s, slow under machine load + concurrent
  build, but green; identical count to tick 27 → zero Python regressions).
- `tsc --noEmit` → clean (whole web project).
- `npm test` → **213 passed, 0 failed** (162 baseline + 51 new).

## Notes / gotchas for next tick
- `next build` reproducibly **stalls at worker-bootstrap** (0% CPU before compiling
  any route) on this host with Node 25.9 + Next 15.5. Killing a build can orphan a
  child `next build` node that holds the `.next` lock and blocks the next run —
  `pkill -f "next build"` before retrying. Did NOT block the push: tsc + 213 web
  tests are the reliable web gates and both passed.
- Frontend backlog: 15 items remain (F6, F9-F12, F14-F16, F19-F25). Next obvious
  picks: F6 (EmptyState rollout to notifications/webhooks/admin/digest — pairs with
  this tick's skeleton/toast consolidation theme), F9 (shot-detail keyboard nav),
  F12 (stats confidence histogram), F19 (dark-mode-aware recharts), F24 (filter
  breadcrumb — `describeFilters` already exists).
- Toast + skeleton are now shared primitives; future pages should reuse them rather
  than rolling bespoke `flash` state or `animate-pulse` divs.
