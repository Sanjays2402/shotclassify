[project]
name = "shotclassify-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["rq", "redis"]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["app"]
